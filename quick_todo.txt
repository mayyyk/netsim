czemu usuwamy słowa extern z funcki defaul_probablity_generator? 

dlaczego jest preferences_t&? tak samo ReceiverPreferences&? czy chodzi o to, że map działa jak tablica i trzeba przekazać wskaźnik na pierwszy element?

dlaczego w receiverpreferences ani ramp ani worker ani storeghouse nie definiujemy konstruktora domyślnego i nie ma explicit? 

czym różni się receiver_preferences_ od preferences_?

czemu packageSender nie ma destruktora?

jak skonfiguorać config.hpp?

czemu używamy unique_ptr w storehouse?

czym różni się * od & np. w tym fragmencie kodu?IPackageReceiver* choose_receiver();



        /**

         * @brief Zwraca mapę preferencji (tylko do odczytu).

         */

        const preferences_t& get_preferences() const;?

czemu jest auto& pair w pętlach for z preferencjes? czy to też przechowuje wskaźniki/referencje? czemu np. nie ma auto* pair? 

co oznacza -> tutaj:     if(!preferences_.empty()) return preferences_.rbegin()->first;?

jaka jest logika push_package w package sender? czyj jest bufer?





CONSISTENT NAMING:
- package/product

SET BETTER AUTO FORMATTING

READ AND CREATE UNDERSTANDABLE TESTS